<nav class="navbar navbar-expand-lg navbar-dark fixed-top" style="background-color: rgb(0, 63, 140);">
    <div class="container">
        <a class="navbar-brand" href="/">
            <img src="/Assets/images/ontario_tech.png" alt="Ontario Tech logo" width="30" height="30" class="d-inline-block align-top">
            Daniel Borrell Diaz
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto">
                <li class="nav-item">
                    <% if (title === 'Home') { %>
                        <a class="nav-link active" href="/index"><i class="fa-solid fa-house"></i> Home</a>
                    <% } else { %>
                        <a class="nav-link" href="/index"><i class="fa-solid fa-house"></i> Home</a>
                    <% } %>
                </li>
                <li class="nav-item">
                    <% if (title === 'Form') { %>
                        <a class="nav-link active" href="/contact"><i class="fa-regular fa-address-card"></i> Contact</a>
                    <% } else { %>
                        <a class="nav-link" href="/contact"><i class="fa-regular fa-address-card"></i> Contact</a>
                    <% } %>
                </li>
                <li class="nav-item">
                    <% if (title === 'Project') { %>
                        <a class="nav-link active" href="/project"><i class="fa-solid fa-list-check"></i> Project</a>
                    <% } else { %>
                        <a class="nav-link" href="/project"><i class="fa-solid fa-list-check"></i> Project</a>
                    <% } %>
                </li>
                <li class="nav-item">
                    <% if (title === 'About') { %>
                        <a class="nav-link active" href="/about"><i class="fa-solid fa-user"></i> About</a>
                    <% } else { %>
                        <a class="nav-link" href="/about"><i class="fa-solid fa-user"></i> About</a>
                    <% } %>
                </li>
            </ul>
        </div>
    </div>
</nav>
